const Show = (props) => {
  return (
    <div className="exp-show-container">
      <div className="show-container">
        <img src={props.image} alt="img" />
      </div>
    </div>
  );
};

export default Show;
